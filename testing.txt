fix banned users cant be promoted to admins 
fix error codes in login because all of them  show " 403 You have been banned by an admin." 
--u can checkthem one by one if username exists and password incorrect return "passwrod not correct"
--if username does not exist return "username does not exist"
--if user has banned boolean true  return "you have been banned by an admin"
register check if the user exists if it does return "username already exists"


 outsiders shouldnt see like svg  
 comments arent deletable by  their authors 
 likes dont appear in notifications

 replace delete post with normal svg like i have on home component on class="list-item report-item" actions and hide svg  to the dashboard list-item report-item action so admins hide posts
speaking about reports can u inhance the class="list-item report-item" and add media preview on right part of card svgs of button actions on top corner and order the meta date make it to the right of the card and 


Are reports on profiles/posts saved with reasons and timestamps?

~Is the password stored using a hashing algorithm (e.g., BCrypt)?

Are all actions (post/report/follow)confirmed with visual feedback?(add follow/unfollow, posted, removed, reported,seccsefully)
Is there a user-friendly UI for reporting users with a reason?
Can the admin view all users, posts, and submitted reports?

Are admin actions confirmed before they take effect? (add confirem action to hide action)
Are invalid routes or actions handled with proper error messages?
Is the browser console free of errors?
Do users receive notifications when followed profiles publish posts?


im havine issues with requerment of that dashboard inhance the class="list-item report-item" add media preview on right part of card button actions on top corner and order the meta date make it to the right of the card 
because i had this as requerments 
Can the admin view all users, posts, and submitted reports?
add svg hide button to dashboard card 
list-item report-item

when spamming like it goes to 500 internal server error in console 
when posting a long title it goes  500 value too long in consol instead it show show alert that title is too long,
as well as comment when commenting with lots of character it goes 500 value too long shouldnt be visible in console and show alert instead
make sure that post content max limit characters is less than 3000(show it as the user is typing in the contentinput 0/3000 if it crosses it change the colorof (i/3000) to red and show -i/3000) and overflow is handling the long texts no scroll just go to next line